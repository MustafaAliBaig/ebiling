# .env.example (commit this; keep your real .env ignored)
@"
# Postgres
POSTGRES_USER=ebilling
POSTGRES_PASSWORD=ebilling
POSTGRES_DB=ebilling
POSTGRES_PORT=15432

# Host-exposed ports
KAFKA_HOST_PORT=9094
REDIS_HOST_PORT=6380
MAILHOG_SMTP_PORT=2525
MAILHOG_HTTP_PORT=8125

# If running app on host (not docker)
KAFKA_BOOTSTRAP_SERVERS=localhost:9094
REDIS_HOST=localhost
REDIS_PORT="${REDIS_HOST_PORT}"
MAIL_HOST=localhost
MAIL_PORT="${MAILHOG_SMTP_PORT}"
"@ | Set-Content -Encoding UTF8 .env.example